{
  "name": "sop-kemenag",
  "version": "1.0.0",
  "private": true,
  "description": "E-SOP Digital Kemenag Barito Utara - Monorepo (Astro FE + Go Fiber BE)",
  "scripts": {
    "dev": "concurrently -k -n FRONTEND,BACKEND -c cyan,magenta \"npm:dev:frontend\" \"npm:dev:backend\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "build": "npm --prefix frontend run build && npm --prefix backend run build",
    "start": "concurrently -k -n FRONTEND,BACKEND -c cyan,magenta \"npm --prefix frontend run start\" \"npm --prefix backend run start\"",
    "install:all": "npm --prefix frontend install && npm --prefix backend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
